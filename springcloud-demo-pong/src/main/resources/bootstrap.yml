spring:
  cloud:
    config:
      discovery:
        enabled: true
        service-id: springcloud-demo-config-server
      profile: dev
      name: aaron-config

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/